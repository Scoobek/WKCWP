/**
 * Sanity content languages. Mirrors the front-end locales in
 * `src/i18n/routing.ts` — keep the two in sync.
 */
export const supportedLanguages = [
  { id: "pl", title: "Polski" },
  { id: "en", title: "English" },
];

export const defaultLanguage = "pl";
